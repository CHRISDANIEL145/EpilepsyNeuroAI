services:
  - type: web
    name: epilepsy-neuro-ai
    env: python
    buildCommand: "python -m pip install -r web_app/requirements.txt"
    startCommand: "cd web_app && gunicorn app:app"
    plan: free
